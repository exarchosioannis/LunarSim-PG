# Ignore generated third-party contents in this directory.
*

# Keep traversing directories so required module rules can be tracked.
!*/

# Keep module rules, this ignore file, and the download manifest.
!*.Build.cs
!.gitignore
!ttp_manifest.json
